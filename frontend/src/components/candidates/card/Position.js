import React from "react";

export default function Position({ children }) {
  return (
    // <p style={{ fontWeight: "bold", fontSize: "1.3rem", margin: "0 2%" }}>
    <p style={{ fontWeight: "bold", fontSize: "1.3rem" }}>{children}</p>
  );
}
